import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shiksha.blackboard',
  appName: 'infinite-blackboard',
  webDir: 'dist'
};

export default config;
